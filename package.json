{
  "name": "quantfi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:unit": "PYTHONPATH=. python3 -m pytest tests -q",
    "test:frontend": "cd frontend && npx vitest run",
    "test:e2e": "npx playwright test",
    "test": "npm run test:unit && npm run test:frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
